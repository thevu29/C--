#include <iostream>
using namespace std;
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) 
{
	int M[3]; //arr[n] la mang co n phan tu tu 0 toi n-1 
	M[0] = 5;
	M[1] = 2;
	M[2] = 3;
	cout << M[0] << " " << M[2] << " " << M[1] << endl;
	int so[]= {4, 5, 6, 2};
	cout << "So[1] = " << so[1];
	return 0;
}
